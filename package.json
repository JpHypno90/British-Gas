{
  "name": "design-tokens",
  "private": true,
  "description": "Design tokens monorepo \u2014 one package per brand + platform.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces",
    "check:primitives": "node scripts/check-primitives.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@tokens-studio/sd-transforms": "^2.0.3",
    "style-dictionary": "^5.4.1"
  },
  "overrides": {
    "@types/node": "^20"
  }
}
